// rank —— 检索结果的相关性打分与排序
//
// 纯函数、零依赖、零网络：打分规则因此可以被逐条单元测试。
//
// 打分只用 mooncakes.io **真实返回**的字段（名称、关键词、描述、下载量、is_new）。
// 该 API 不提供「最后更新时间」，所以这里不把「维护活跃度」当作信号 ——
// 排序依据必须是能落地的东西。

import {
  "liguanda888/mooneco/mooncakes",
}
