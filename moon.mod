// MoonContext MCP — 模块配置
// 文档：https://docs.moonbitlang.com/en/latest/toolchain/moon/module.html
//
// 新增依赖：moon add <module>

name = "liguanda888/mooncontext"

version = "0.1.0"

readme = "README.md"

repository = "https://github.com/liguanda888/mooncontext-mcp"

license = "Apache-2.0"

keywords = [ "mcp", "moonbit", "ai", "agent", "ecosystem" ]

description = "MCP server giving AI coding agents accurate MoonBit ecosystem knowledge and token-budgeted project context"

preferred_target = "native"
